[] We should support zooming in and scrolling the waveform (i.e. to hone in on a specific section)
[] The audio play preview should support looping (toggleable, so we can either let it cut off or repeat to the start). Ideally, this would include the fade, but if that would require running an ffmpeg command every time, we don't need to. If an ffmpeg execution would be required, then instead we should add a "generate full preview" button which will add a component to the page (not a blocking pop up!) with a loading bar and then eventually can just click play (and/or toggle the looping) to listen to the full preview.